/**
 * Logos 博客前台配置文件
 */
const CONFIG = {
    // API 基础URL
    API_BASE_URL: 'http://localhost:3000',
    
    // 每页显示的文章数量
    POSTS_PER_PAGE: 10,
    
    // 每页显示的评论数量
    COMMENTS_PER_PAGE: 10,
    
    // 日期格式化选项
    DATE_FORMAT_OPTIONS: {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
    }
};
